# 兩階段監督式微調（SFT / CoT-SFT）完整實驗紀錄

Causal Mask 需要做出來 plot sft 都要 展示mask

## 一、研究背景與核心問題

本研究目標是建立一個在有限算力條件下仍可穩定訓練、同時兼具通用指令能力與推理能力的中文論文級訓練流程。實務上，若直接以小規模推理資料進行微調，模型容易出現語言流暢度下降、任務覆蓋不足與過擬合問題；若只使用一般指令資料，則推理深度不足。基於此，本研究採用兩階段監督式微調策略：先做通用能力對齊，再做推理能力專化。

在早期實驗中，我們遭遇了嚴重的 **Token space drift + role collapse + multilingual garbage decoding**（外語碎片、亂碼、語義飄移）問題。經查證，根本原因是 **Chat format 混用**：模型同時接收了 `<s>...</s>` (LLaMA style)、`<|im_start|>` (ChatML) 與 `<final>` (自訂推理) 等多種對話系統標籤，導致訓練與推論的格式不一致。

為解決此問題，我們對整個資料集管線進行了 **ChatML-only 格式統一**，徹底移除了所有衝突的標籤，確保訓練與推論使用完全同構的前綴。

## 二、資料集組成與分布

### 2.1 資料集名稱與來源對應

為避免實驗紀錄與論文敘述脫節，以下先定義本研究中實際使用的資料集名稱與來源關係：

- `alpaca_hf`：由 Alpaca 原始 instruction/input/output 欄位轉為對話格式後得到的一般指令資料集。
- `ultrachat_200k/hf`：UltraChat 200k 來源資料的對話快照。
- `mix_a25_u75_hf`：將 `alpaca_hf` 與 `ultrachat_200k/hf` 依實際可用筆數混合後得到的第一版混合集（**純 ChatML 格式**）。
- `mix_a25_u75_cot_hf`：第一階段主訓練資料集（確保無 `<final>` 污染的純淨版本）。
- `sft_cot_hf`：第二階段推理專化資料（每筆含問題、推理內容與最終答案）轉為訓練格式後的資料集。

對應到訓練階段：

- 第一階段（General SFT）使用 `mix_a25_u75_cot_hf`。
- 第二階段（CoT-SFT）使用 `sft_cot_hf`。

第一階段資料為混合指令語料，來源由一般指令資料與大規模對話資料構成。混合後可用樣本總數為 **251,974** 筆，來源分布如下：

- 一般指令資料：約 **20.6%**
- 大規模對話資料：約 **79.4%**

經純 ChatML 格式標準化後，總量約 **302,597,070** tokens。所有 25 萬筆樣本均已通過嚴格檢查，保證 0 筆含有 `<s>`、`</s>` 或 `<final>` 標籤。

## 三、資料格式與標註策略

本研究使用絕對一致的 ChatML 對話模板，明確保留使用者與助理輪次邊界，使模型在訓練中持續學習角色切換與回合終止條件。

### 3.1 核心 Token 與 Mask 策略

在 SFT 訓練中，損失函數（Loss）的計算範圍至關重要。我們僅對 Assistant 的回答內容與結束符進行學習（計算 Loss），其餘部分皆遮罩（Mask，Label = -100）。

| 文本內容              | 這是什麼 Token？      | 該不該 Mask？ (Label ID) | 為什麼？                                                          |
| :-------------------- | :-------------------- | :----------------------- | :---------------------------------------------------------------- |
| `<\|im_start\|>`      | 特殊 Token (對話起點) | 要 Mask (-100)           | 這是系統格式，模型不用預測它。                                    |
| `user\n`              | 一般 Token            | 要 Mask (-100)           | 同上。                                                            |
| `請總結這篇文章...`   | 一般 Token (User問題) | 要 Mask (-100)           | 模型不需要學會「如何問問題」，這是給它看的情境。                  |
| `<\|im_end\|>\n`      | 特殊 Token (User結束) | 要 Mask (-100)           | 屬於 User 區塊的結尾。                                            |
| `<\|im_start\|>`      | 特殊 Token (對話起點) | 要 Mask (-100)           | 這是 Prompt 的最後一個結尾標籤，用來觸發模型回答。                |
| `assistant\n`         | 一般 Token            | 要 Mask (-100)           | 同上，告訴模型「換你講話了」。                                    |
| `這篇文章的重點是...` | 一般 Token (AI回答)   | 🟢 **不 Mask (算 Loss)** | 這是真正的回答內容，當然要學。                                    |
| `<\|im_end\|>`        | 特殊 Token (結束符)   | 🟢 **不 Mask (算 Loss)** | 重點！ 模型必須學會「何時閉嘴」。如果不學這個，它會無限生成下去。 |

### 3.2 SFT Mask 機制的底層數學原理

從自迴歸語言模型（Autoregressive Language Model）的底層數學原理來看，Mask 在訓練中扮演了分離「條件（Prompt）」與「目標（Answer）」的關鍵角色。

#### 1. 預訓練與 SFT 的目標差異

語言模型的本質是給定上下文來預測下一個字。假設有一段長度為 $T$ 的 Token 序列 $X = (x_1, x_2, \dots, x_T)$，根據連鎖律，序列的聯合機率為：
$$P(X) = \prod_{t=1}^{T} P(x_t | x_{<t}; \Theta)$$

在**預訓練階段**，目標是最大化整段文本的對數似然（Log-Likelihood），即最小化 Cross-Entropy Loss：
$$\mathcal{L}_{Pretrain} = - \frac{1}{T} \sum_{t=1}^{T-1} \log P(x_{t+1} | x_{\le t}; \Theta)$$
這代表**序列中的每一個字都要參與計算**（學習預測 Prompt 與預測 Answer）。

然而，在 **SFT（監督式微調）階段**，我們不希望模型去學習預測人類給定的 Prompt ($P$)。我們真正要優化的目標是**給定 Prompt 的條件下，生成 Answer ($A$) 的條件機率**：
$$P(A | P) = \prod_{t=k}^{T-1} P(x_{t+1} | x_{\le t}; \Theta)$$

#### 2. Mask 的引入與反向傳播意義

為實作此目標，我們引入二元遮罩向量 $M_t \in \{0, 1\}$：

- 當 $x_{t+1}$ 屬於 Prompt 區塊時，$M_t = 0$（即 PyTorch 中的 `ignore_index = -100`）。
- 當 $x_{t+1}$ 屬於 Answer 區塊時，$M_t = 1$。

SFT 的 Loss 函數即轉變為：
$$\mathcal{L}_{SFT} = - \frac{1}{\sum_{t} M_t} \sum_{t=1}^{T-1} M_t \cdot \log P(x_{t+1} | x_{\le t}; \Theta)$$

當我們對模型權重 $\Theta$ 求梯度時：
$$\frac{\partial \mathcal{L}_{SFT}}{\partial \Theta} \propto \sum_{t=1}^{T-1} M_t \cdot \frac{\partial}{\partial \Theta} \left[ - \log P(x_{t+1} | x_{\le t}; \Theta) \right]$$
User 區塊的 Loss 雖然是 0，但未來的誤差會順著隱藏狀態（Hidden State）逆流回來更新底層特徵。
這個數學式直接解釋了 Mask 的物理意義：

1. **$M_t = 0$ 區塊（Prompt）**：梯度直接歸零。模型在該位置不會有任何權重更新。這確保了模型不會浪費算力去背誦使用者的問題。
2. **$M_t = 1$ 區塊（Answer 與 `<|im_end|>`）**：梯度正常回傳。優化器會迫使 $P(x_{t+1} | x_{\le t}; \Theta) \to 1$。
3. **結束符的必要性**：在 Logits 輸出層的 $V$ 維空間中，如果不對 `<|im_end|>` 算 Loss（設為 $M_t = 0$），目標函數就不會懲罰該維度的低機率。模型講完話時，會發現預測標點符號的機率高於 `<|im_end|>`，導致無窮盡的亂碼生成。必須設定 $M_t = 1$，模型才能學會在正確時機將結束符的 Logit 推到最高。
   🔥 進階觀念：User 區塊的權重「完全」不會更新嗎？
   這是一個面試常考的陷阱題！答案是：User 區塊的字，依然會獲得梯度更新，但「原因」不同！

這聽起來很矛盾，請看以下解釋：

不會因為「預測 User」而更新：如同前面所說，我們不要求模型學會預測 User 的問題，這部分的梯度是 0。

會因為「作為 Assistant 的上下文」而更新：

當模型在生成 Assistant 的答案時，它的「注意力機制（Attention）」會去「看」前面 User 講過的話。

為了讓 Assistant 答得更好（這部分有算 Loss），反向傳播的梯度會從 Assistant 的位置出發，沿著 Attention 的連線，一路倒流回到 User 字詞的特徵向量（Embeddings）上。

### 3.3 ChatML-only 資料轉換模板

所有的異質資料來源都被嚴格映射到同一對話語法。對於單輪問答，標準化後的模板為：

```text
<|im_start|>user
...使用者內容...<|im_end|>
<|im_start|>assistant
...助理答案...<|im_end|>
```

在推論（Inference）時，我們只餵給模型開放式的 Assistant 前綴，讓它接續生成：

```text
<|im_start|>user
...使用者內容...<|im_end|>
<|im_start|>assistant
```

多輪對話則依此結構重複交替。這個極簡且統一的設計使模型在訓練期間穩定了：  
（1）角色切換、（2）回合收束。

### 3.4 多輪對話的連續學習機制 (Multi-turn Continuous Learning)

在真實的對話場景中，使用者與模型的互動往往包含多個輪次（Multi-turn）。本研究並未將多輪對話人為拆解成獨立的單輪樣本，而是將其視為**單一筆連續的長序列**進行端到端訓練。在此架構下，模型能依據因果注意力機制（Causal Attention）看見前面的歷史上下文，同時透過精準的 Mask 矩陣，確保僅對「每一輪的助理回答」計算 Loss。

#### 1. 多輪對話的區塊對齊實作

以實際訓練集中的一筆 4 輪對話為例，資料在底層會被解析為單一字串，並在其中包含 4 個回答區段：

```text
第 1 段：<|im_start|>assistant\n1. Influencer marketing: Companies are collaborating...<|im_end|>
第 2 段：<|im_start|>assistant\nAs an AI language model, I don't have personal opinions...<|im_end|>
第 3 段：<|im_start|>assistant\nAs an AI language model, I can say that relying solely on traditional...<|im_end|>
第 4 段：<|im_start|>assistant\nAs an AI language model, I can say that keeping up with the latest marketing trends...<|im_end|>
```

在資料前處理階段，我們實作了字元級別的對齊邏輯（Character-level Masking & Token Alignment）：

1. **區塊定位**：透過正則表達式（`re.DOTALL`）全局掃描整段序列，精準提取出所有 `assistant` 回答區塊的字元邊界。
2. **字元遮罩（Character Mask）**：初始化一個與序列等長的二元陣列（預設為不參與 Loss 計算），並將上述提取出的 Assistant 區塊範圍設為生效（True）。此時所有 User 提問與系統標籤皆維持 Mask 狀態。
3. **標記映射（Token Alignment）**：利用 Tokenizer 的 `return_offsets_mapping=True` 特性，將字元遮罩精準映射回 Token 級別。若某個預測目標 Token 所涵蓋的原始字元中，有任何一個落在 Assistant 區塊內，該 Token 即參與 Loss 計算；反之則設為 `-100`。

這種機制的優勢在於，在同一個 Forward Pass 中，模型能夠完整吸收 4 次交替的上下文語境，並同時產生 4 個回合的梯度更新，最大化了多輪推理的學習效率。

#### 2. 長序列尾部截斷風險 (Truncation Risk)

連續學習機制的一個潛在風險在於**尾部截斷**。當多輪對話的總 Token 數超過系統設定的最大序列長度（如 $SEQ\_LEN=2048$）時，程式會執行前向截斷。

若截斷點剛好落在最後一輪對話的中間，尤其是切斷了最後的 `<|im_end|>` 標籤，模型在該筆樣本中將無法學習到「回答完畢需主動閉嘴」的收斂訊號。因此，在設定訓練序列長度時，必須透過數據統計（如確保多數樣本的長度落入 $SEQ\_LEN$ 內）來控制截斷率，以維持終止符號學習的穩定度。

## 四、第一階段（General SFT）訓練設計

第一階段任務是建立穩定的通用能力底座，包括指令遵循、多輪對話穩定度與基礎語言品質。基於資料長度分布與硬體限制，本研究將主要序列長度設定為 2048；在此設定下，能有效涵蓋多數對話長度。

### 第一階段主要訓練參數

- 序列長度：2048
- 每卡批次大小：1
- 梯度累積：16（等效批次為 16）
- 初始學習率：1e-5
- 訓練輪數：3 epochs
- checkpoint 週期：每 100 steps 儲存
- 路由溫度：固定 0.5（降低路由退火引起之訓練震盪）

此配置的本質是以小批次加梯度累積維持訓練穩定，並盡可能保留長上下文資訊。

## 五、第二階段（CoT-SFT）訓練設計

第二階段使用具推理監督訊號的資料（每筆含問題、推理內容與最終答案）進行專化。其目的不是取代第一階段，而是在既有通用能力上增強多步推理一致性、結構化答案能力與推理可控性。

### 第二階段主要訓練參數

- 序列長度：512
- 每卡批次大小：8
- 梯度累積：4（等效批次為 32）
- 訓練輪數：100 epochs
- 驗證頻率：每 15 steps

相較第一階段，第二階段採較短序列與較高更新密度，重點在快速強化推理模式，而非再次學習通用語言分布。

## 六、兩階段銜接策略

本研究採「先廣後專」的能力分工：

- 第一階段：建立通用對齊能力（指令理解、語言流暢、多輪穩定）。
- 第二階段：專化推理能力（步驟化思考、結構化輸出、推理一致性）。

第二階段以第一階段模型參數初始化，可有效降低小資料從頭訓練的過擬合風險，並維持通用語言能力不被快速覆蓋。

## 七、目前訓練進度與推論除錯建議

模型在早期如果出現「亂碼、網址、無關字元」，其核心問題已被證明為**推論輸入格式與訓練格式不一致**。

自從我們將資料集全部切換為純 ChatML 格式後，推論急救模板必須嚴格對齊：

```text
<|im_start|>user
Who are you?<|im_end|>
<|im_start|>assistant
```

_(注意：末行 assistant 之後必須換行，不可省略)_

推論時建議採較保守採樣：

- `temperature = 0.2`
- `top_p = 0.85`
- `max_new_tokens = 64 ~ 128`

## 八、方法有效性與總結

- **解決崩潰問題**：徹底清除了 LLaMA BOS/EOS 與自訂 `<final>` 帶來的 Token Space 污染，解決了角色混亂與解碼亂碼。
- **資料分工明確**：第一階段（General SFT）建立純淨的 ChatML 對話底座，不插入干擾性的空推理區段；第二階段（CoT-SFT）專注於推演邏輯。

本研究建立之兩階段 SFT / CoT-SFT 訓練流程，以大規模混合資料（約 25 萬筆）完成穩定對齊，目前格式清洗已完全落實，為後續實驗奠定了乾淨穩定的基礎。

幫我加入stf 說明這樣 需要多一章節書名，跟 需要書在料分布等等，有給資料跟我底下有顯示 改成 加入特殊token 跟 模型看到的數字 (Input IDs ㄋ 對應的切詞結果 可以先用html 展示 在截圖圖片這樣需要論文那樣設計

整體先 ins mode 在cot 這樣兩階端，

自定義資料及完全符合 9.8 章節之設計

底下是素材需要轉換 成論文版本

──────────────────────────────────────── 🧠 SFT 訓練與 Packing 建議 ───────────────────────────────────────────╮
│ 1. 資料集總 Token 量: 248,435 tokens │
│ 2. 設定的最大長度 (seq_len): 1024 │
│ │
│ 3. 截斷風險 (Truncation): │
│ 有 1 筆資料 (0.1%) 超過 1024 tokens。 │
│ │
│ 4. 訓練有效比例 (Loss Masking): │
│ 全集平均有效學習比例為 71.8%。(目標：Assistant 回應占比越高越好) │
│ 有 0 筆資料的有效學習 Token 佔比不到 30%，可能拖慢訓練效率。 │
│ │
│ 5. 拼接策略 (Token Packing): │
│ 開啟 Packing 後，大約會將 1032 筆資料壓縮成 243 個訓練 Steps。 │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯

# LLM 監督微調資料建構與 SFT 設定（技術說明）

![SFT training plots](output/sft_train_val_plots.png)

---

## 摘要

本儲存庫現行預設管線為：自 **Alpaca** 與 **UltraChat（200k）** 來源抽取子集合 → 將對話 **單輪化**（每列僅含一對 user–assistant）→ 於建檔階段套用 **`ins_strict` 指令品質過濾** → 輸出 **`mix_a25_u75_ins`**（HF 快照 + uint16 token 串流）→ 以 ChatML 對齊之監督微調。詞表大小 **32 007**；語言模型文字編碼與專案 tokenizer 設定一致。

---

## 1. 符號與計數約定

| 符號               | 意義                                                                                                                                         |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| \(N\)              | 建檔完成後之 **監督樣本數**（HF 列數），即「文本列」筆數                                                                                     |
| \(T\)              | 對全集 \(N\) 筆 `text` 以專案 tokenizer **`encode(..., add_special_tokens=False)`** 後之 **子詞 token 總數**，等同 `.bin` 串流長度（uint16） |
| \(\bar{\ell}=T/N\) | 每筆樣本平均 token 長度（全資料平均，非抽樣）                                                                                                |
| \(V\)              | 詞表大小，\(V=32007\)                                                                                                                        |

**說明：** 過濾步驟僅在「是否收錄該列」上作用；**未被收錄之列不進入 \(N\) 與 \(T\)**。本版 **`mix_a25_u75_ins_meta.json`** 未另存「過濾前」總列數；若要報告剔除比例，須另建一次 **`--instruction-filter off`** 之對照集並比較列數，或於建檔腳本外加計數器。

---

## 2. 資料來源與混合比例

原始快照（見 meta）：

- **Alpaca**：`dataset/alpaca_hf`，總列數 \(N\_{\mathrm{A}}^{\mathrm{pool}}=51\,974\)。
- **UltraChat**：`dataset/ultrachat_200k/hf`，總列數 \(N\_{\mathrm{U}}^{\mathrm{pool}}=200\,000\)。

混合策略（建檔程式預設與下列數學式一致）：在目標比例約 **Alpaca : UltraChat ≈ 25 : 75** 下，對 Alpaca **不放回抽樣**至多 \( \lfloor \frac{r}{1-r} N\_{\mathrm{U}} \rfloor \) 列（\(r\approx0.25\)），本建檔使用 **該上限所允許之全數 Alpaca**（即 **51 974** 列全部使用），UltraChat **全數保留**。  
由此得到「進入單輪化與過濾管線」之 **對話列數**為 **251 974**；經 **單輪展開** 後列數增加，再經 **`ins_strict`** 篩選後得到最終 \(N\)（下節）。

---

## 3. 前處理管線

### 3.1 單輪展開（建檔選項：`--single-turn`）

令多輪對話序列为 \([u_1,a_1,u_2,a_2,\ldots]\)。單輪展開將其分解為若干監督範例，每範例為一對 \((u_k,a_k)\) 經固定 ChatML 模板串接（每範例獨立一列）。Alpaca 來源多為單輪，列數變化小；UltraChat 多輪來源之列數可於展開後 **倍增**。

---

### 3.2 指令品質過濾：模式總覽

過濾僅依 **第一段 user 區塊**之文本判定；**不**修改 assistant。模式如下表（皆為確定性規則，無隨機種子）。

| 模式           | 行為摘要                                                                  |
| -------------- | ------------------------------------------------------------------------- |
| **off**        | 不剔除；所有已展開之列皆入庫。                                            |
| **drop_weak**  | 剔除符合 §3.7「弱起手／極短灌水」之 user；其餘保留。                      |
| **ins_strict** | 若 user 屬 Alpaca 指令模板（§3.4），依該節規則；否則依 §3.5（一般對話）。 |

**再現性：** 同一來源快照與相同模式字串下，過濾結果可重複。

---

### 3.3 User 區塊抽取（形式化輸入）

給定一整段監督文本 \(s\)（單輪 ChatML），令 **第一段 user 區塊** \(u\) 為：由片段 `<|im_start|>`、`user`、換行之後起算，至首次出現結束標記 **`<|redacted_im_end|>`**（長形式閉合標記；與本文資料材料化管線之 ChatML 序列化輸出一致）或相容標記 **`<|im_end|>`**（短形式；指令過濾模組正則表達式所允許之備援閉合）止之中間文本；首尾空白去除後記為 \(u\)。若無法抽取或 \(u\) 為空字串，該列在 **off** 以外之模式下 **拒絕**。

以下凡稱「句首」皆指 \(u\) **去除前導空白後**之字串；**不分大小寫**之比對皆透過正則 `IGNORECASE` 實現。

---

### 3.4 分支 A：Alpaca 型「指令區」（`### Instruction:`）

若 \(u\) 含子字串 `### Instruction:`，定義 **指令核心** \(I(u)\) 為：自該標題之後至下一個 `### Input:` 或 `### Response:`（若出現）之前之子字串，並去除首尾空白。若 \(I(u)=\emptyset\) 則視同無有效指令區。

| 子模式         | 接受條件（依序判斷；滿足任一即 **接受**）                                                                                                                                                                                                                                                     |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **drop_weak**  | \(I(u)\) 之全文 **不**與 §3.7 表 B「整段弱起手」或表 C「尾句弱請求」匹配（對 \(I(u)\) 做折疊空白後比對）。                                                                                                                                                                                    |
| **ins_strict** | 對 \(I(u)\) **依序**檢驗，滿足任一即 **接受**：（1）\(\lvert I(u)\rvert \ge 16\)（字元數）；（2）\(I(u)\) 符合 §3.6「指令／問句開頭」；（3）\(I(u)\) 中空格字元數對應之「詞間分隔」使詞數意義下可視為 \(\ge 4\) 詞（實作以 `plain.count(" ") >= 3` 判定）；（4）\(\lvert I(u)\rvert \ge 8\)。 |

---

### 3.5 分支 B：一般 user（無有效 Alpaca 指令區；典型為 UltraChat 單輪）

#### 3.5.1 `drop_weak`

令 \(u_1\) 為 \(u\) 之 **第一行**（以換行分割）；令 \(\mathrm{collapse}(u)\) 為將 \(u\) 內連續空白壓成單一空格後之字串。**拒絕**若以下任一成立：

- \(\mathrm{collapse}(u_1)\) 匹配表 B「整段弱起手」；
- \(\mathrm{collapse}(u_1)\) 匹配表 C「尾句弱請求」；
- \(\mathrm{collapse}(u)\) 全文匹配表 B；
- §3.7 表 D「極短灌水」為真。

否則 **接受**。

#### 3.5.2 `ins_strict`

**接受**若滿足以下 **任一**條件：

1. **指令／問句開頭**：\(u\)（strip 後）符合 §3.6 之句首模式（自字串開頭匹配）。
2. **長問答緩解**：\(\lvert u\rvert\_{\mathrm{strip}} \ge 120\) **且**（\(u\) 含字元 `?` **或** 含子字串 \`\`\` 三反引號程式區標記）。

否則 **拒絕**。

（註：分支 B 下 **不**再套用表 B/C 之否定式；`ins_strict` 以「正面列表 + 長度／標點緩解」為主，弱起手主要由 **未滿足接受條件** 而間接排除。）

---

### 3.6 句首「指令／問句」模式（`ins_strict` 正面列表）

句首判斷為：字串開頭（允許前導空白）至首次匹配下列 **詞界 \`\b\`** 類別之一，其後可接任意後綴。下表為完整枚舉（正則中 \`summar(?:ize|ise|y)\` 涵蓋 _summarize / summarise / summary_）。

| 類別           | 允許之句首 token 或片語（不分大小寫）                                                                                                                                                                                                                                                                                                                                                                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WH-            | `what`, `why`, `how`, `when`, `where`, `which`, `who`, `whose`                                                                                                                                                                                                                                                                                                                                                |
| 任務動詞       | `explain`, `describe`, `list`, `outline`, `summarize`, `summarise`, `summary`, `compare`, `contrast`, `define`, `identify`, `analyze`, `analyse`, `discuss`, `evaluate`, `justify`, `predict`, `recommend`, `calculate`, `compute`, `solve`, `prove`, `derive`, `simplify`, `write`, `compose`, `draft`, `rewrite`, `translate`, `convert`, `formulate`, `state`, `name`, `give`, `show`, `find`, `determine` |
| 述題／助動開頭 | `is`, `are`, `was`, `were`, `can`, `could`, `would`, `should`, `does`, `do`, `did`, `has`, `have`, `had`, `will`                                                                                                                                                                                                                                                                                              |
| 禮貌／祈使片語 | `could you` 或 `can you`，後接可選 `please`，再接下列之一：`explain`, `describe`, `list`, `summarize`, `summarise`, `tell`, `help`, `expand`                                                                                                                                                                                                                                                                  |
| 祈使 `please`  | `please` 後接下列之一：`explain`, `describe`, `list`, `summarize`, `summarise`, `write`, `analyze`, `answer`, `provide`, `show`                                                                                                                                                                                                                                                                               |
| 其他固定片語   | `define whether`, `in detail`, `step by step`                                                                                                                                                                                                                                                                                                                                                                 |

---

### 3.7 弱起手與極短灌水（`drop_weak` 及 Alpaca `drop_weak` 否定式）

#### 表 B：整段弱起手（全文須 **完全**匹配該類問候／寒暄pattern；允許句尾標點 `! ? . …`）

類別含：**簡短問候**（hi, hello, hey, yo, sup, hiya, morning, hi there, hey there）、**寒暄**（how are you, how's it going, how have you been, what's up, wassup, whats good）、**空洞請求**（tell me something, say something, tell me anything, say more, tell me more）、**時段問候**（good morning/afternoon/evening/night）、**致謝告別**（thanks, thank you, thx, ty, bye, goodbye, cya, see you）、**社交套話**（nice to meet you, how do you do, you okay, you alright）。

#### 表 C：尾句弱請求（整行匹配）

模式：句首為 `tell` 或 `give`，接 `me`，再接 `something`、`anything` 或 `more`，句尾可選標點。

#### 表 D：極短灌水判準

| 規則編號 | 條件                                                                                                                                          | 判定             |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| D1       | \(\mathrm{len}(u) > 56\)                                                                                                                      | 不視為灌水       |
| D2       | \(\mathrm{len}(u) \le 26\) **且** 詞數 \(\le 6\) **且** 各詞（去標點）皆屬表 E **填充詞集合**（或詞數 \(\le 2\) 且詞集合 \(\subseteq\) 表 E） | **灌水（拒絕）** |
| D3       | \(\mathrm{len}(u) < 72\) **且**（小寫後以 `tell ` 或 `give ` 開頭）**且**含子字串 `something`                                                 | **灌水（拒絕）** |

#### 表 E：填充詞集合（共 **25** 詞）

`go`, `on`, `ok`, `okay`, `cool`, `nice`, `really`, `sure`, `yeah`, `yep`, `wow`, `interesting`, `continue`, `more`, `else`, `right`, `so`, `and`, `then`, `what`, `hmm`, `uh`, `um`, `lol`, `haha`。

---

### 3.8 訓練階段是否再次過濾

若建檔已使用 **`ins_strict`**，建議監督訓練時將指令過濾設為 **off**，以免對同一靜態資料集 **重複套用**相同規則（徒增計算，結果不變）。

---

### 3.9 參數閾值彙總表（便於實驗對照）

| 符號／名稱                                      | 數值            | 適用                |
| ----------------------------------------------- | --------------- | ------------------- |
| Alpaca 指令核心最小保留長 \(\lvert I(u)\rvert\) | **16**          | `ins_strict` 分支 A |
| Alpaca 指令核心備援最短長                       | **8**           | `ins_strict` 分支 A |
| Alpaca 指令核心最少空格數                       | **3**           | `ins_strict` 分支 A |
| 一般 user 長問答長度下界                        | **120**（字元） | `ins_strict` 分支 B |
| 極短灌水長度上界（總則）                        | **56**          | 表 D                |
| 極短灌水細判（長度／詞數）                      | **26**／**6**   | 表 D-D2             |
| tell/give + something 長度上界                  | **72**          | 表 D-D3             |

---

## 4. 資料集統計：`mix_a25_u75_ins`

數值來自 **`dataset/mix_a25_u75_ins_meta.json`**（建檔當下寫入）；token 總數與 `.bin` 檔一致。

| 項目                               | 數值                                                                                                                  |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 監督樣本數 \(N\)                   | **796 133**                                                                                                           |
| Token 總數 \(T\)                   | **245 162 791**                                                                                                       |
| 平均每筆長度 \(\bar{\ell}=T/N\)    | **≈ 307.94** tokens                                                                                                   |
| `.bin` 大小（uint16，理論）        | \(2T\) bytes **≈ 0.49 GB**                                                                                            |
| 來源對話列用量（建檔前、未單輪化） | Alpaca **51 974**、UltraChat **200 000**（`alpaca_used` / `ultrachat_used`）                                          |
| 來源混合比（對話列計）             | `mix_ratio_alpaca` ≈ **0.206**、`mix_ratio_ultrachat` ≈ **0.794**（= `alpaca_used / (alpaca_used + ultrachat_used)`） |

**註：** **最終 \(N=796\,133\)** 為「單輪展開 + `ins_strict`」後之 **文本列** 數；因 UltraChat 多輪列會展成多列，**\(N\)** 可顯著大於來源對話列數 **251 974**。Alpaca 多為單輪，對 **\(N\)** 的貢獻接近「通過過濾之 Alpaca 文本列數」，細項未單獨寫入 meta。

---

## 5. 長度分佈（抽樣估計，供 `SEQ_LEN` 選型）

對 **`mix_a25_u75_ins_hf`** 隨機抽 **80 000** 列（seed=0），以 **與訓練相同**之 encode 設定量測每筆長度：

- **min / mean / max**（tokens）：約 **18 / 308 / 1262**
- **分位數**：p50 ≈ **320**，p90 ≈ **434**，p95 ≈ **468**，p99 ≈ **546**
- **截斷率** \(P(\mathrm{len} > \texttt{SEQ_LEN}+1)\) 約：**SEQ_LEN=512 → ~2%**；**768 → ~0.07%**

完整設定下訓練啟動會寫入 **`output/sft_hf_text_len_stats*.json`**（檔名依 `SFT_SINGLE_TURN_EXPAND`、`SFT_INSTRUCTION_FILTER` 變體）；路徑錨定專案根目錄。

---

## 6. 訓練／驗證劃分（`train_sft`）

預設自 **HF 列索引** 隨機劃分 **5%** 做驗證（`VAL_FRAC`，種子 `VAL_SEED`），**展開後樣本數**隨單輪／過濾而變；驗證集與訓練集使用 **相同** ChatML loss mask。精確 `|D_{\mathrm{train}}|`、`|D_{\mathrm{val}}|` 可由訓練日誌首段「展開後 samples」讀取。

---

## 7. 實作命令與環境（再現性）

| 階段            | 建議命令／設定                                                     | 備註                                |
| --------------- | ------------------------------------------------------------------ | ----------------------------------- |
| 虛擬環境        | Conda 環境名 **`py310`**（或以環境變數 `ENV_NAME` 覆寫）           | 與背景訓練腳本預設一致              |
| Python 模組路徑 | `PYTHONPATH` 須包含儲存庫下 **`scripts`** 目錄                     | 以便匯入訓練與資料模組              |
| 建混合資料      | `python3 scripts/build_mix_alpaca_ultrachat.py`（參數見 §2、§3.1） | 於儲存庫根目錄執行                  |
| 監督微調        | `python3 scripts/train_sft.py`                                     | 預設讀取 §4 所指資料與 §1 tokenizer |
| 背景訓練        | `./run_train_sft_bg.sh`                                            | 同樣假設於儲存庫根目錄執行          |
| 載入權重覆寫    | 環境變數 **`SFT_CHECKPOINT_LOAD`**                                 | 未設定時依訓練程式內建優先序        |

文字格式與特殊符號：§1、§2；損失僅作用於 assistant 區段之設定見訓練程式文件字串說明。

---

## 8. 附錄：舊版 Mixed CoT（`mix_dyn`）

若使用 **`build_mix_cot_format.py`** 將混合資料轉為 **`<final>`** 包覆之 CoT 外殼（`mix_a25_u75_dyn_*`），token 分佈與截斷需求與 **§4–§5** 之 **`mix_ins`** **不可直接相比**；長對話多時常需較大 **`SEQ_LEN`**（例如 2048）。歷史抽樣 raw 可見 `dataset/mix_dyn_10sample_raw_20260427.txt` 等檔。

---

## 9. 其他工具

- **互動推理**：`./run_sft_cli.sh`
- **曲線繪製**：`scripts/plot_sft_train_val.py`，輸入 `output/train_sft_log.csv`、`output/val_sft_log.csv`
